//
//  CustomElements.swift
//  Multiplicity
//
//  Created by David Lorow on 10/18/24.
//

import SwiftUI

extension Color /* Red */ {
    static let neonRed = Color(red: 1.0, green: 0.1, blue: 0.1)

    static let lightRed = Color(red: 1.0, green: 0.7, blue: 0.7)

    static let extraLightRed = Color(red: 1.0, green: 0.85, blue: 0.85)
}

extension Color /* Orange */ {
    static let neonOrange = Color(red: 1.0, green: 0.5, blue: 0.0) // Bright neon orange
    
    static let lightOrange = Color(red: 1.0, green: 0.8, blue: 0.5) // Light orange
    
    static let extraLightOrange = Color(red: 1.0, green: 0.9, blue: 0.8) // Extra light orange
}

extension Color /* Blue */ {
    static let neonBlue = Color(red: 0.1, green: 0.5, blue: 1.0) // Bright neon blue
    
    static let lightBlue = Color(red: 0.7, green: 0.8, blue: 1.0) // Light blue
    
    static let extraLightBlue = Color(red: 0.85, green: 0.9, blue: 1.0) // Extra light blue
}

extension Color /* Purple */ {
    static let neonPurple = Color(red: 0.7, green: 0.1, blue: 1.0)
    
    static let lightPurple = Color(red: 0.8, green: 0.7, blue: 1.0)
    
    static let extraLightPurple = Color(red: 0.9, green: 0.85, blue: 1.0)
}

extension Color /* Green */ {
    static let neonGreen = Color(red: 0.3, green: 0.9, blue: 0.0)
}
